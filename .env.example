### Environment example for docker-compose (do NOT commit real secrets)
# Copy to `.env` and fill values, or inject via your secret manager/CI.

# Postgres
POSTGRES_DB=fiop
POSTGRES_USER=fiop_user
# Set a strong password in production; do NOT commit the real value.
POSTGRES_PASSWORD=

# Optional: you can provide a file containing the password and reference it
# from your deployment tooling if supported (e.g. Docker secrets).
POSTGRES_PASSWORD_FILE=

# SEC API key for vendor integrations
SEC_API_KEY=

# Application
LOG_LEVEL=INFO
WORM_STORAGE_PATH=/app/data/worm
PYTHONPATH=/app/src

# Redis (default compose uses internal redis service)
REDIS_URL=redis://redis:6379/0

# Override the full DB URL if desired (takes precedence over POSTGRES_* in some setups)
# DATABASE_URL=postgres://fiop_user:secret@postgres/fiop

# Notes:
# - Never store plaintext secrets in the repo. Use OS secret stores, Vault, or GitHub Actions Secrets.
# - When running locally, copy this file to `.env` and fill values: `cp .env.example .env`
# - CI should inject secrets (e.g. `POSTGRES_PASSWORD`, `SEC_API_KEY`) through protected variables.
